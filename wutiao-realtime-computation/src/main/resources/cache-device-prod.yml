keyPrefix: ""                                           # 默认前缀为空，根据需求配置不同的前缀
retryNum: 1                                             # 默认为1，可选值 1/2/3
retryIdle: 1000                                         # 重试间隔，默认是1000毫秒，类型是int
needHash: false
items:
    -
        cacheLevel: 1
        cacheType: mysql
        props:
            dbUrl: jdbc:mysql://172.27.2.72:3306/wutiao?useSSL=false
            username: datac                             # 数据库访问用户名
            password: g3Z2zHF6uTxK6#rnlZ7               # 数据库访问密码
            table: realtime_new_device                  # 数据表，如果不存在，会自动创建，前提有自动创建权限
            keyColumnName: did                          # 可选 默认为key
            valueColumnName: info                       # 可选 默认为value
            batchSize: 1000                             # 可选 默认为1000
            writeBack : true                            # 可选 默认为false
            keyPartition: 20                            # 可选 默认为20
            needHash: false                             # 可选 默认为true
            autoCreateTable: false                       # 可选 默认为true